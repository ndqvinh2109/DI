package com.cosa.di.service;

public class FirstServiceImpl implements FirstService {
    public void printSomething() {
        System.out.println("Print out something in FirstServiceImpl" + this);
    }
}
